# react-intro-quiz-notes

## Quiz Questions

Answer the following questions in the provided markdown file before turning in this exercise:

- What is React?
  - React is a library designed to simplify the creation of functional components
- What is a React component?
  - a React component is a piece of the UI with its own logic and appearance
- How do you mount a React app (root component) to the DOM?
  - using the createRoot function
- What are some other popular frontend frameworks?
  - angular, react, vue.js, ember.js

## Notes

All student notes should be written here.

How to write `Code Examples` in markdown

for JS:

```javascript
const data = 'Howdy';
```

for HTML:

```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:

```css
div {
  width: 100%;
}
```
