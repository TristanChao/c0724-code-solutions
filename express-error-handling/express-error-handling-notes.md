# express-error-handling-notes

## Notes

All student notes should be written here.

- always put error-handling middleware last
- error-handling middleware is defined by having 4 parameters
  - it it doesn't have all 4 (error, request, response, next), it cannot be detected as an error handler

How to write `Code Examples` in markdown

for JS:

```javascript
const data = 'Howdy';
```

for HTML:

```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:

```css
div {
  width: 100%;
}
```
